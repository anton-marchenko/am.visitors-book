const { Visit } = require('../../../models/visit');

describe('visit - createNewVisit()', () => {
    it.todo('should save visit if it is valid input');
    it.todo('should return an error if an user is not found for given cardId');
});
