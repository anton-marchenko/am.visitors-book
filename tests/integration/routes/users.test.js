describe('/api/users', () => {
    describe('GET /', () => {
        it.todo('should return 401 if client is not logged in');
        it.todo('should return 403 if user is not admin');
        it.todo('should return 200 if it is a valid request');
        it.todo('should return all users');
    });
});
