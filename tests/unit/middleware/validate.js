const { validate } = require('../../../middleware/validate');

describe('validate middleware', () => {
    it.todo('should return 400 if there is an invalid input');
    it.todo('should call next() if there is a valid input');
});